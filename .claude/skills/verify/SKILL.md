Run the full test suite once (no background/parallel builds).
Report pass/fail counts and list any failures including pre-existing ones.
Do NOT claim success until the suite is green or the user confirms.
Only then stage changes in logical commits.